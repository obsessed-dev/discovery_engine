[package]
name = "discovery"
version = "0.1.0"
edition = "2024"

[lib]
name = "discovery"
path = "src/lib.rs"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
mac-vendor-lookup = { path = "../vendor" }
pnet = "0.35.0"
