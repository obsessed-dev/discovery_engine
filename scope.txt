Milestone 5 — Integration, Validation & Test Suite
The discovery engine is feature complete with ARP and ICMP sweep, vendor resolution, latency tracking, and comfy-table output confirmed working against a Docker test network. Milestone 5 is about hardening, automated testing, and CI/CD readiness.
Phase 1 — Unit Tests (no root required, pure logic):

[DONE] Merge/dedup logic — 6 tests passing: icmp_only_has_no_mac, arp_only_has_mac, same_ip_merges_to_both, merged_host_preserves_icmp_latency, no_duplicate_ips_in_output, output_is_sorted_by_ip
[DONE] CIDR parsing and subnet iteration — /24 host count, /24 skips network address, /31 has 2 hosts, /32 has 1 host
[DONE] Vendor lookup — 7 tests passing: known_vendor_full_mac_uppercase, known_vendor_full_mac_lowercase, known_vendor_full_mac_mixed_case, known_vendor_oui_only, known_vendor_second_entry, unknown_vendor_returns_none, unknown_oui_only_returns_none
[SKIPPED] HostRecord construction — plain data struct, no invariants to validate; fields exercised across merge tests

Phase 2 — Integration Tests (assert_cmd + Docker): [DONE]

[DONE] assert_cmd + predicates added as dev-dependencies
[DONE] tests/integration.rs — DockerFixture RAII struct handles setup/teardown via Drop, defensive pre-teardown prevents "network already exists" noise on repeat runs
[DONE] integration_discovers_docker_network — asserts binary exits success, table headers present, 2 hosts discovered, 192.168.99.2 + 192.168.99.3 in stdout, exactly 2 IP rows (no duplicates)
[DONE] Runs clean on repeat passes — confirmed consistent
[DONE] Run with: sudo -s, export RUSTUP_HOME/CARGO_HOME/PATH, cargo test --test integration -- --ignored

Phase 3 — Hardening: [DONE]

[DONE] Replace remaining unwrap/expect calls with proper error propagation — ok_or on icmp packet constructors, expect messages on Arc::try_unwrap chains, unwrap_or_else with mutex poison recovery and warn! logging
[DONE] Add log crate + env_logger — log/env_logger in Cargo.toml, use log::{debug,info,warn} in lib.rs, env_logger::init() in main.rs, RUST_LOG=level sudo ./discovery works correctly
[DONE] No new unit tests required — hardening changes are network-bound or don't affect observable behavior, existing suite still complete
[TODO] Finalize README — libpcap-dev dependency, sudo requirement, Docker test instructions, bash script usage

Phase 4 — Jenkins Pipeline (local Jenkins server):

cargo build --release
cargo test for unit tests (no privileges)
Docker network spin up via bash scripts
Integration tests with CAP_NET_RAW granted to test binary
Docker teardown
Archive release binary as build artifact
Run pipeline in loop for stress testing threading, merge logic, and timeout behavior

Current output confirmed working:

Interface, network, host count header
comfy-table with IP, MAC, Vendor, Latency, Method columns
Both method rendering correctly for dual ARP+ICMP hits
Latency column populated
Vendor showing dash for unknown MACs as expected

Crates in use: pnet, ipnetwork, clap, comfy-table, mac_vendor_lookup (local path dep, future crates.io publish)